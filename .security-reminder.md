# 🚨 CRITICAL SECURITY REMINDER

## Never Expose Keys

**I will NEVER:**
- Show actual API keys, secrets, or tokens in chat
- Display keys in terminal output
- Include real keys in documentation
- Share credentials in any form

**I will ALWAYS:**
- Use placeholders (`your_key_here`, `YOUR_SECRET_KEY`)
- Mask keys in examples
- Check output before displaying
- Use environment variable references instead of actual values

## If a Key is Accidentally Exposed

1. **Immediately rotate the key** in the service dashboard
2. **Update all locations** (Vercel, `.env.local`, etc.)
3. **Verify** everything still works
4. **Document** the rotation in `SECURITY_KEY_ROTATION.md`

---

**Last Incident**: Supabase anon key exposed in terminal output - rotation required
**Status**: See `SECURITY_KEY_ROTATION.md` for rotation instructions


